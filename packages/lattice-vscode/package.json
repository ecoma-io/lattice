{
  "name": "lattice",
  "displayName": "Lattice — polyglot module boundaries",
  "description": "Module-boundary diagnostics in the buffer for Go, Rust, Python and Vue, read from your Nx workspace's own boundary config",
  "version": "0.11.2",
  "private": true,
  "publisher": "ecoma-io",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./extension.mjs",
  "icon": "icon.png",
  "categories": [
    "Linters",
    "Programming Languages"
  ],
  "keywords": [
    "nx",
    "monorepo",
    "module-boundaries",
    "polyglot",
    "language-server"
  ],
  "engines": {
    "vscode": "^1.100.0",
    "node": ">=22"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Lattice",
      "properties": {
        "lattice.server.path": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "markdownDescription": "Path to the boundary server's `lsp.mjs`. Relative paths resolve against the Nx workspace root.\n\nLeave this empty unless you are developing the server itself. Empty means the `@ecoma-io/lattice` installed in the workspace — the same version your pipeline runs, and therefore the only one whose verdicts are guaranteed to match it.\n\nA path that does not exist is reported rather than ignored: this extension will not quietly fall back to a different server than the one you named."
        },
        "lattice.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Log the conversation with the boundary server to the Lattice output channel. The channel's log-level dropdown (Output panel gear icon) is the master switch: set it to Trace, then choose messages or verbose here."
        }
      }
    },
    "commands": [
      {
        "command": "lattice.restart",
        "title": "Restart the boundary server",
        "category": "Lattice"
      },
      {
        "command": "lattice.showLog",
        "title": "Show the log",
        "category": "Lattice"
      }
    ]
  },
  "dependencies": {
    "vscode-languageclient": "10.1.0"
  },
  "devDependencies": {
    "@types/vscode": "1.100.0",
    "@vscode/vsce": "3.9.2"
  },
  "homepage": "https://github.com/ecoma-io/lattice/tree/main/packages/lattice-vscode#readme",
  "bugs": {
    "url": "https://github.com/ecoma-io/lattice/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ecoma-io/lattice.git",
    "directory": "packages/lattice-vscode"
  }
}
